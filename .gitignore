# 敏感金鑰 - 絕對不能上傳到 GitHub
backend/serviceAccountKey.json

# Firestore 資料匯出（含 auth_users 的 email/passwordHash 等個資憑證，絕不上傳）
firestore_export/

# Python cache
backend/__pycache__/
backend/**/__pycache__/
*.pyc
*.pyo

# Node
node_modules/
.env
.firebase/

# OS
.DS_Store
Thumbs.db

# Local temp profiling output
new_excel_profile.json

# 圖片爬蟲衍生產物（可重新產生，不入庫避免 repo 肥大）
downloaded_images/
download_report.csv

# 本地商品檔與備份（含成本價，不入庫避免外洩；正式模式讀 Firestore）
products_local.json
products_local.backup_*.json

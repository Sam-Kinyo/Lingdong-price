rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: 後續改成登入後依角色控管
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
